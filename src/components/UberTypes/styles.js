import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        paddingHorizontal: 20
    },
    confirm:{
        width : '100%',
        backgroundColor: '#15e7df',
        paddingVertical: 10,
        alignItems :'center'
    },
    confirmText: {
        color : 'white',
        fontWeight: 'bold'
    }
});

export default styles;
